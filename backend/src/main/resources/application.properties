# H2 DB
# PUTS DATA IN A FILE usersDB IN A MAP /data/DB IN YOUR PROJECT
spring.datasource.url = jdbc:h2:file:./data/db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=test
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.datasource.name=plantDB
# FOR DEBUGGING INFO

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=car4rent.team15@gmail.com
spring.mail.password=gsgtwpebwxaesyvi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

custom.fill=true
custom.sendMail=false

#minio

spring.minio.url=http://localhost:9000
# Minio Bucket name for your application
spring.minio.bucket=storage
# Minio access key (login)
spring.minio.access-key=minio-root-user
# Minio secret key (password)
spring.minio.secret-key=minio-root-password
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=5000MB
# Max Request Size
spring.servlet.multipart.max-request-size=5000MB

